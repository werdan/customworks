<?php

/**
 * @file
 */

// add menu to footer menu region
// if (in_array($vars['elements']['#region'], array('menu'))) {
//   $theme = alpha_get_theme();

//   switch ($vars['elements']['#region']) {
//     case 'menu':
//       $vars['title'] = $theme->page['title'];
//       break;
//   }
// }
